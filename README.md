# UpdateChecker
UpdateChecker is a portable Windows app that scans for program (Winget) and driver (PowerShell/WMI) updates, installs them one-by-one with automatic system restore points, and logs everything to a local SQLite database. Features dark-themed GUI, toast notifications, history filtering/export, and cloud sync to Synology WebDAV and GitHub
